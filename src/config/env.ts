import {BACKEND_URL, SOCKET_URL} from '@env';

const resolvedBackendUrl = BACKEND_URL;
const resolvedSocketUrl = SOCKET_URL;

export {resolvedBackendUrl as BACKEND_URL, resolvedSocketUrl as SOCKET_URL};
